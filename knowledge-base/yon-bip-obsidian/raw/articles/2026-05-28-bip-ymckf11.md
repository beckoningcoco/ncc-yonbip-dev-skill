YMC开发实践
最后更新时间：2025-11-25
YMC介绍
产品简介

新一代 YonBIP 监控中心，简称 YMC。以实时监控、快速诊断定位问题为主要特点，通过主机-微服务-容器-中间件多级资源监控、微服务全链路监控、实时线程监控分析、BPR 业务录制诊断和云端远程监控等能力，为 YonBIP 提供从公有云到专属云的多样化监控与问题诊断分析服务。

YMC 模块主要包括 YMC-Link（中控台），YMC-Portal（门户），YMC-Proxy（客户端代理），同时集成了哈勃监控的日志分析、报警、监控指标、链路分析、运维工具、性能分析功能等模块能力。 采用云加端的架构设计，实现远程实时监控客户项目现场系统，方便发现、分析现场项目问题。

整体架构

稳定性排查方法
报警查询看板

主动发现

报警查询看板（全局发现性能瓶颈）

报警通知

被动通知

报警通知：是一种用于监控和告警的机制。它的核心功能在于通过预设的规则和条件，对特定指标或事件进行实时检测，并在满足告警条件时触发相应的告警动作

报警策略

配置策略

报警策略：是一种用于监控和告警的机制。它的核心功能在于通过预设的规则和条件，对特定指标或事件进行实时检测，并在满足告警条件时触发相应的告警动作

容器CPU问题-CPU利用率或负载高
微服务监控

访问路径：首页->指标监控->微服务监控，录入对应的微服务编码

利用线程堆栈
主动 点击生成选择容器实例生成线程堆栈;
被动 超过容器CPU利用率报警阈值，自动生成线程堆栈）

火焰图
主动 点击生成按钮选择容器实例生成火焰图；
被动 超过CPU利用率报警策略阈值后，自动生成火焰图；

y 轴表示调用栈，每一层都是一个函数。调用栈越深，火焰就越高，顶部就是正在执行的函数，下方都是它的父函数。
x 轴表示抽样数，如果一个函数在 x 轴占据的宽度越宽，就表示它被抽到的次数多，即执行的时间长。注意，x 轴不代表时间，而是所有的调用栈合并后，按字母顺序排列的。
鼠标可以点击的选中的每个框就代表了一个栈里的函数，其宽度可以直接理解为CPU时间占比（其实是采样的数量以及与采样总量的占比），那么，也就是说占比比较宽的框就表示：a.该函数运行时间较长（单次时间长）b.被调用次数较多.（调用频率高）进而被采样的次数比较多，占用的CPU时间多。
Arthas命令排查
进入Arthas控制台

进入YMC首页，在页面顶部位置找到点击 指标监控->微服务监控，录入微服务信息，进入找到实例列表，点击控制台后，点击Arthas进入（指标监控->微服务监控->实例列表->控制台->Arthas)

生成线程堆栈快照

方法一：

dashboard ##确定哪个线程占用cpu过高。

thread pid ##查看具体线程和线程堆栈信息，定位出现问题的代码，pid为线程id。

方法二：

thread -n xxx ##查看哪个线程占比最高

生成火焰图

profiler start 开始

profiler getSamples 采样

profiler stop --format html --file /root/test/xxx.html 输出 html 格式的火焰图

跟踪定位问题

monitor -c 2 xxx xxx

watch & jad

Linux命令排查
进入控制台

进入YMC首页，在页面顶部位置找到指标监控，点击找到微服务监控并进入，录入微服务名称或编码，进入容器列表页面，点击控制台进入（指标监控->微服务监控->实例列表->控制台）

生成线程堆栈快照

1、top ##查看当前占用CPU较高的进程PID

2、使用 top -H -p PID 找出该进程内占用CPU最多的线程。

3、使用 printf "%x\n" tid 将线程tid转换为16进制格式

4、使用 jstack PID | grep tid 命令，其中 tid 是你转换得到的16进制线程ID。

容器内存问题 - POD被驱逐（内存配置不足）

堆内存初始值（-Xms）和最大值（-Xmx）：

2048 MB

非堆内存消耗：

256 MB (包括元空间、直接内存等)

操作系统和其他开销：

128 MB

突发内存需求：

512 MB （业务高峰；突发情况）

Pod最小内存 = 2048 MB (堆内存初始值)+256 MB (非堆内存消耗)+128 MB (操作系统和其他开销)

= 2432 MB

Pod最大内存 = 2048 MB (堆内存最大值)+256 MB (非堆内存消耗)+128 MB (操作系统和其他开销)+512 MB (突发内存需求)

= 2944 MB

JVM问题 - GC频繁
GC频繁

点击 性能工具 ->JVM监控面板 ，选择实例，查看微服务实例JVM内存情况

堆对象
主动 点击生成按钮选择容器实例生成堆对象;
2.被动 超过容器内存利用率报警策略阈值后，自动生成堆对象

jmap -histo 7276 | head -n20

火焰图
主动 点击生成按钮选择容器实例生成火焰图；
被动 超过容器内存利用率报警策略阈值后，自动生成火焰图

在火焰图中寻找宽度较大的块，这些块代表了内存分配的热点，即内存分配较多的代码路径。这些热点可能是内存泄漏或内存消耗过多的原因。

堆内存Dump
主动

点击 指标监控->微服务监控，录入微服务信息，进入找到实例列表，点击控制台后，找到Dump堆内存，生成完后点击下载堆内存

注：主动堆转储过程中会暂停程序 ，需要先进行故障隔离再dump（可在技术平台微服务实例上进行故障隔离操作）

堆内存dump文件路径： -XX:HeapDumpPath=

发生OutOfMemoryError错误时生成堆内存dump文件：XX:+HeapDumpOnOutOfMemoryError

被动

堆内存OOM后，自动生成堆内存dump

Arthas命令排查
进入Arthas控制台
进入YMC首页，在页面顶部位置找到点击 指标监控->微服务监控，录入微服务信息，进入找到实例列表，点击控制台后，点击Arthas进入（指标监控->微服务监控->实例列表->控制台->Arthas)
生成堆转储文件：
heapdump arthas-output/dump.hprof ## dump 到指定文件
heapdump --live /tmp/dump.hprof ## 只 dump live 对象
查看JVM内存信息

memory

生成火焰图（或通过控制台右上角生成火焰图直接生成）：
profiler start -e alloc ##开始
profiler getSamples ##采样
profiler stop --format html --file /root/test/xxx.html ##输出 html 格式的火焰图
JVM问题 - 元空间内存溢出

元空间趋势图：性能工具->JVM监控面板

jstat -gc (结果列MC:元空间的容量；结果列MU:元空间的使用量)

最小元空间 & 最大元空间：-XX:MetaspaceSize=512m -XX:MaxMetaspaceSize=512m

追踪类加载 & 类卸载：-XX:TraceClassLoading -XX:TraceClassUnloading

微服务问题 - 接口响应慢

看链路全貌

1、哈勃录制（按照以下截图步骤进行哈勃录制，然后进行业务操作，最后在第2步一样的位置进行结束哈勃录制）

2、查看BPR详情 & 哈勃报告（ymc置顶位置BPR tab页可查看）

BPR（先看全局概要信息，了解可能的定性瓶颈点，分析后发现慢查询占比较高，可通过左侧导航进入SQL耗时TOP部分）

BPR-微服务链路（点击耗时点sql，可进入sql详情查看具体sql）

trace跟踪

数据库问题 - 慢查询
定位慢查询

慢查询警告看板

通过mysql进程的信息查看

1、select id from information_schema.processlist where time >5 and info!=''; （查询大于5s的所有慢查询）

2、info列中的内容为sql语句

通过mysql进程的信息查看

1、select id from information_schema.processlist where time >5 and info!=''; （查询大于5s的所有慢查询）

2、info列中的内容为sql语句

优化慢查询

explain + 慢sql

select_type：代表表示查询中每个select子句的类型，是简单查询还是联合查询还是子查询。
type：表示MySQL在表中查找所需数据的方式。依次从最优到最差分别为：system > const > eq_ref > ref > range > index > ALL
possible_keys：应该或建议使用的索引。表示MySQL能使用哪个索引在表中找到记录，查询涉及到的字段上若存在索引，则该索引将被列出，但不一定被查询使用。这个趋向于指导性作用。
key：实际使用的索引。显示MySQL在查询中实际使用的索引，若没有使用索引，显示为NULL，即全局扫描。
key_len：这一列显示了 mysql 在索引里使用的字节数。
ref：显示索引的哪一列被使用了，如果可能的话，是一个常数。哪些列或常量被用于查找索引列上的值，常见的有：const（常量），字段名（例：iuap_apdoc_basedoc.grp.id）
rows：列显示MySQL认为它执行查询时必须检查的行数。Rows的行数数值越少越好。
filtered：这个字段表示存储引擎返回的数据在server层过滤后，剩下多少满足查询的记录数量的比例，注意是百分比，不是具体记录数。