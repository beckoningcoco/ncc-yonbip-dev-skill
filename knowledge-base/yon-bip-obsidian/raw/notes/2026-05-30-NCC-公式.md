---
url: https://community.yonyou.com/datadict/bipbook/chapter1504/chapter1504.html
title: "公式 | YonBIP高级版"
description: "YonBIP高级版电子书"
language: "zh"
captured_at: "2026-05-29T18:06:53.396Z"
---

# 公式 | YonBIP高级版

## 公式

---

## 1 YonBIP高级版公式简介

用户使用产品时，往往对单据上的字段取值有各种不同的需求。为此YonBIP高级版提供了模板公式功能，可以让实施顾问或者用户通过配置各种公式，并且在不用修改代码的前提下满足用户的各种个性化需求。 YonBIP高级版模板公式按使用功能可以分为编辑公式、显示公式、验证公式。

## 1.1 编辑公式

可实现单据编辑中字段自动带值的功能。只在编辑状态起作用，在某字段上设置好编辑公式，当此字段编辑后，公式会被执行，并将结果填充到目的字段上。 例如：人员助记码设置为由业务单元编码+人员编码组成，可以设置编辑公式将取到的编码拼接后设置到人员助记码字段上。

## 1.2 显示公式

实现单据显示时加载非持久信息的功能。只在非编辑状态起作用，例如查询后加载或保存后刷新界面等，模板上的显示公式均会执行，运算结果会显示到相应字段上。 例如：采购入库单取采购订单的预计到货日期。

## 1.3 验证公式

可实现单据保存前的检查功能。一般在单据保存时执行所有字段上的验证公式，根据公式类型的不同做不同的响应，可能弹出错误信息，也可能弹出确认信息等。起到验证及提示作用，项目上应用广泛。

例如：对输入数据内容进行校验，如果不符合需求则提示错误信息。

## 2 三大公式开发配置

## 2.1 显示公式开发配置

各领域查询时会执行显示公式，此处为显示公式的前端配置：

![](https://community.yonyou.com/datadict/bipbook/assets/2-1.BjbrsfBX.png)

关于props.dealFormulamsg( formulamsg, tableTypeObj )：

参数：formulamsg 公式的数组，值是控制公式的信息，如果没有配置控制公式，则这个值为空

tableTypeObj 界面上所有表格的类型，必输

后端不同的单据类型会对应不同的Handler，处理公式需要在Action的excute()方法中新建Handler对象，并传入视图数据和界面模板，为后续校验提供参数。

| **单据类型** | **调用的方法** |
| --- | --- |
| 表单单据 | FormFormulaHandler.handleLoadFormula() |
| 列表单据 | GridFormulaHandler.handleLoadFormula() |
| 一主一子表头 | BillCardFormulaHandler.handleLoadFormula() |
| 一主一子表体 | BillCardFormulaHandler.handleBodyLoadFormula() |
| 一主多子表头 | ExtBillCardFormulaHandler.handleLoadFormula() |
| 一主多子表体 | ExtBillCardFormulaHandler.handleBodyLoadFormula() |

![](https://community.yonyou.com/datadict/bipbook/assets/2-2.D8dMErI6.png)

在“应用注册”节点下找到对应节点的模板，并在“页面模板注册”中点击“设置页面模板”按钮进入相应的模板设置界面，如下图所示：

![](https://community.yonyou.com/datadict/bipbook/assets/2-3.COIqfK38.png)

1、点击字段名，会有对应的“显示属性”和“高级属性”两个设置属性的区域。

2、在“高级属性”中，有对应的“显示公式”、“编辑公式”和“验证公式”，点击显示公式右侧放大镜即可对显示公式进行配置。

![](https://community.yonyou.com/datadict/bipbook/assets/2-4.DU03Cpx4.png)

此处在“公式编辑器”中将显示公式配置为当前的业务日期：

![](https://community.yonyou.com/datadict/bipbook/assets/2-6.DPNGCDu2.png)

修改当前业务日期并在人员节点查看人员工作信息，配置过显示公式的“任职结束日期”已经变为当前业务日期。

![](https://community.yonyou.com/datadict/bipbook/assets/2-7.q1_tm3Bw.png)

## 2.2编辑公式开发配置

编辑公式的使用，需要创建节点时，在平台提供的 createPage 方法中提供节点参数 billinfo，进行前端适配。正确适配的页面，如果页面上的某个字段设置了元数据关联项或者编辑公式，会走平台的编辑后事件，平台的编辑后事件里会进行编辑关联项和编辑公式的处理。

billinfo传参示例：

![](https://community.yonyou.com/datadict/bipbook/assets/2-8.Bl9jLQiR.png)

在某些情况下如果需要主动调用平台的编辑后方法，可以通过this.props.handleRelationItems方法，该方法的调用格式、参数说明如下：

![](https://community.yonyou.com/datadict/bipbook/assets/2-9.BRG5HWzB.png)

同显示公式配置，在页面模板设置中找到需要添加编辑公式的字段，点击编辑公式右侧放大镜，打开公式编辑器，即可进行编辑。例如在物料税类模板中，设置名称由编码自动带出，则可以设置税类编码的编辑公式如下：

mattaxes.mattaxesname->mattaxes.mattaxescode

![](https://community.yonyou.com/datadict/bipbook/assets/2-10.DL9lL4IG.png)

当界面处于编辑态时，税类名称会根据税类编码自动带出：

![](https://community.yonyou.com/datadict/bipbook/assets/2-11.B7fm8aXg.png)

## 2.3验证公式开发配置

前端在保存时先执行props.validateToSave函数进行公式验证，如果验证成功则调用callback函数调用ajax进行数据保存， 格式如下：

`props.validateToSave(data, callback, tableTypeObj, billType)`

参数说明：

data：单据数据，必输

tableTypeObj：界面上所有表格的类型，必输

callback：点击确认的时候执行的自定义回调，非必输

billType：单据类型，非必输

![](https://community.yonyou.com/datadict/bipbook/assets/2-12.C6J_5YhW.png) 在页面模板中找到相应的字段配置验证公式

![](https://community.yonyou.com/datadict/bipbook/assets/2-13.DX2hhEze.png)

例如，人员助记码设置验证公式为：

`$Error->iif(psndoc_card_head.code==psndoc_card_head.mnecode ,"","人员助记码应该为人员编码！")`

![](https://community.yonyou.com/datadict/bipbook/assets/2-15.hvKmQHjb.png)

点击保存时会先进行公式验证，如果验证成功会进行保存，如果校验失败会提示配置的错误信息。

![](https://community.yonyou.com/datadict/bipbook/assets/2-14.CFMWoz4D.png)

## 3 自定义公式配置与开发

如果现有的公式不能够满足需要时，可以根据需求自己编写一个公式。开发自定义公式的过程如下：

1、编写java类，该类继承 nc.vo.pub.formulaset.function.NcInnerFunction;

在类里需要构造函数中指明公式参数的个数，公式的类型，公式的描述，并覆写function(List param)函数。 其中function(List param)是执行公式时需要调用的代码。公式的类型取值如下所示：

| **公式类型** | **取值** |
| --- | --- |
| 字符串 | IFormulaConstant. FUN\_STRING |
| 数学 | IFormulaConstant. FUN\_MATH |
| 日期 | IFormulaConstant. FUN\_DATE |
| 数据库 | IFormulaConstant. FUN\_DB |
| 打印 | IFormulaConstant. FUN\_PRINT |
| 财务会计 | IFormulaConstant. FUN\_GL |
| 自定义 | IFormulaConstant. FUN\_CUSTOM |
| 集合类 | IFormulaConstant. FUN\_COLLECT |

如下是一个简单的自定义类型公式代码：

![](https://community.yonyou.com/datadict/bipbook/assets/3-1.B0DU_PbU.png)

2、编写配置文件，文件的路径放在 **NCHome\\resources\\formulaconfig\\custfunction** 目录下，文件编写要按照一定格式。例如custfun.xml，编写内容如下

![](https://community.yonyou.com/datadict/bipbook/assets/3-2.Bf1cpGel.png)

3、java代码编写及xml配置部署完毕后重启服务，即可在前端页面中使用公式。

![](https://community.yonyou.com/datadict/bipbook/assets/3-3.BAaTuRYj.png)

## 4 常见公式介绍

单据公式目前主要分为日期,数学,字符串,数据库,财务以及控制公式这6个大分类，如下是一些常用的公式介绍。

## 数据库

| 公式 | 说明 |
| --- | --- |
| ass(freevalueID,checktype) | 关于会计平台中辅助核算的函数,从gl\_freevalue表中根据freevalueID及checktype返回checkvalue |
| getColValue2(tablename,fieldname, pkfield1,bpkvalue1,pkfield2,pkvalue2) | 根据主键从数据库查询特定字段的值,其功能类似SQL语句:select fieldname from tablename where pkfield1 = pkvalue1 and pkfield2 = pkvalue2. 从这条SQL语句可以看出各个参数的含义. |
| cvs(tablename,fieldname,pkfield,pkvalue) | 根据主键从数据库查询特定字段的值,其返回的值将直接作为字符串使用 |
| getColsValue("tablename","fieldname1","fieldname2", "pkfield",pkvalue) | 根据主键从数据库查询多个字段的值，左边待赋值的字段要用逗号分割，注意里面的字段，表名要用双引号扩起来 |
| getColNmv2(tablename,fieldname, pkfield1,pkvalue1,pkfield2,pkvalue2) | 根据主键从数据库查询特定字段的值，其返回的值将直接作为数字使用，其功能类似 SQL 语句：select fieldname from tablename where pkfield1 = pkvalue1 and pkfield2 = pkvalue2; 从这条 SQL 语句可以看出各个参数的含义 |
| getColValue(tablename,fieldname,pkfield,pkvalue) | 根据主键从数据库查询特定字段的值，其功能类似 SQL 语句：select fieldname from tablename where pkfield = pkvalue; 从这条 SQL 语句可以看出各个参数的含义 |
| cvn(tablename,fieldname,pkfield,pkvalue) | 根据主键从数据库查询特定字段的值，其返回的值将直接作为数字使用 |
| getColNmv(tablename,fieldname,pkfield,pkvalue) | 根据主键从数据库查询特定字段的值，其返回的值将直接作为数字使用，其功能类似 SQL 语句：select fieldname from tablename where pkfield = pkvalue; 从这条 SQL 语句可以看出各个参数的含义 |
| getMLCValue("tablename","fieldname","pkfield",pkvalue) | 根据主键从数据库查询特定当前登录语种对应名称字段的值 |

## 数学

| 公式 | 说明 |
| --- | --- |
| sub(num1,num2) | 用于高精度减法运算 |
| ln(x) | 返回给定数值 x 的自然对数 |
| mod(, ) | 求模运算 |
| atanh( ) | atanh(z) = 1/2 \* log( (1+z)/(1-z) ) |
| log(x) | 返回给定数 x 的以十为底的对数 |
| mul(num1,num2) | 用于高精度乘法运算 |
| cos(x) | 返回给定角度x的余弦值 |
| sum(x,y) | 计算两个数的和 |
| getresult() | 计算两个数的和 |
| atan(x) | 返回一个弧度x的反正切值，弧度值在 -Pi/2 到 Pi/2 之间 |
| rand() | 生成随机数 |
| div(num1,num2) | 用于高精度除法运算 |
| tanh( ) | sinh(z) / cosh(z) |
| min(x, y) | 求 x，y 两者中的最小值 |
| sqrt(x) | 返回数值 x 的平方根 |
| angle(, ) | Math.atan2(x.doubleValue(), y.doubleValue()) |
| sin(x) | 返回给定角度 x 的正弦值 |
| sgn(num) | 当数 num 大于 0 时返回 1，等于 0 时返回 0，小于 0 时返回 -1 |
| zeroifnull(var) | 表示如果 var 为空将返回 0 |
| exp(x) | e 的 x 次方 |
| tan(x) | 返回给定角度 x 的正切值 |
| add(num1,num2) | 用于高精度加法运算 |
| max(x, y) | 求数字 x，y 两者中的最大值 |
| sinh( ) | sinh(z)=( exp(z) - exp(-z) ) / 2 |
| acosh( ) | acosh(z) = log(z + sqrt(z\*z - 1)) |
| acos(x) | 返回一个弧度 x 的反余弦，弧度值在 0 到 Pi 之间 |
| int(数字或者字符串) | 将变量转换为 int 类型 |
| cosh( ) | cosh(z) = ( exp(z) + exp(-z) ) / 2 |
| toNumber(String str) | 将字符串 str 转换为本解析器可识别的数字，比如 toNumber(“45.0”)将返回一个数字型 45.0，经过转化后可参与各种数值计算 |
| abs(num) | 求数 num 的绝对值 |
| round(double num, int index) | 对 num 保留 index 位小数(四舍五入) |
| asin(x) | 返回一个弧度 x 的反正弦，弧度值在 -Pi/2 到 Pi/2 之间 |
| asinh(z ) | asinh(z) = log(z + sqrt(z\*z + 1)) |

## 日期

| 公式 | 说明 |
| --- | --- |
| compareDate(date1,date2,field) | 用于日期比较，返回两个日期指定时间域的差值，可比较的时间域包括“Y”-比较年；“M”-比较月；“D”-比较日；“H”-比较小时；“m”-比较分钟；“S”-比较秒。比如：compareDate(“2005-12-27 23:12:10”,toDateTime("2005-12-27 23:12:08"), "S") 将返回两个日期相差的秒数 |
| date() | 返回当前日期 |
| yearof(date) | 求日期 date 的年 |
| year() | 求当前年 |
| toDateTime(str) | 将字符串格式的时间 str 转换成 UFDateTime 对象，比如toDateTime("2006-10-15 21:01:01") |
| toTime(str) | 将字符串格式的时间 str 转换成 UFTime 对象 |
| month() | 求当前月 |
| datetime() | 返回当前日期和时间 |
| toDate(str) | 将字符串格式的时间 str 转换成 UFDate 对象 |
| dateAdd(date1, num, fieldchar) | 返回在指定日期的年、月或者日上增加某个值 num，可增加的时间域fieldchar 包括 “Y”-增加年；“M”-增加月；“D”-增加日；“H”-增加小时；“m”-增加分钟；“S”-增加秒。比如 dateAdd("23:13:23", 1, "H") 表示对前面的时间增加一小时 |
| month(date) | 得到指定日期内的月份 |
| loginbusidate() | 得到当前登录业务时间，前后台均可用，如果是后台使用，可能会得不到 |
| formataddress( ) | 格式化地址，根据地址簿 id 将地址格式化成语言格式中设置的样式 |
| time() | 取得当前时间，格式是 HH:mm:SS |
| dayOf(date) | 求日期 date 的天数 |
| dateFormat(date, pattern\[,language\]) | 用于将时间格式化为期望的字符串，其中 date 可以是时间字符串，也可以是 Date 对象，pattern 为格式化参数，yyyy 表示年，MM 表示月，dd 表示天数，HH 表示小时，mm 表示分钟，ss 表示秒。比如dateFormat("2006-07-04 12:12:12", "日期: yyyy-MM-dd HH:mm:ss") 将返回 "日期: 2006-07-04 12:12:12" |

## 字符串

| 公式 | 说明 |
| --- | --- |
| left(str, index) | 求字符串 str 左边前 index 个字符组成的字符串 |
| right(String str, int index) | 求字符串 str 右边前 index 个字符组成的字符串 |
| leftStr(str,len,defaultStr) | 求字符串 str 左边前 len 个字符组成的字符串，如果字符串长度小于 len，则用 defaultStr 补齐，比如 leftStr("abc",6,"@") 将返回 abc@@@ |
| rightStr(str,len,defaultStr) | 求字符串 str 右边后 len 个字符组成的字符串，如果字符串长度小于 len，则用 defaultStr 补齐，比如 rightStr("abc",6,"@") 将返回 abc@@@ |
| mid(String str, int start, int end) | 求字符串 str 左边前第 start 个字符至第 end 个字符之间的字符串 |
| startsWith(String str, String start) | 判断字符串 str 是否以字符串 start 开头 |
| endswith(str, end) | 判断字符串 str 是否以字符串 end 结尾 |
| isEmpty(str) | 用于判断 str 是否为空，包括空串("")及空值(null) |
| length(str) | 求字符串 str 的长度 |
| toLowerCase(String str) | 求字符串 str 的小写形式，比如 toLowerCase("Abc") 返回 "abc" |
| toUpperCase(String str) | 求字符串 str 的大写形式 |
| indexOf(str1, str2) | 判断字符串 str1 中第一个字符串 str2 所在的位置，比如lastIndexOf("HI,UAP2006,UAP","UAP") 返回 3 |
| lastIndexOf(str1, str2) | 判断字符串 str1 中最后一个字符串 str2 所在的位置，比如lastIndexOf("HI,UAP2006,UAP","UAP") 返回 11 |
| equalsIgnoreCase(str1, str2) | 判断忽略大小写字符串 str1 是否与字符串 str2 相等 |
| toString(obj) | 将对象 obj 转换为本解析器可识别的字符串形式 |
| trimzero() | 剪除字符串或数字 str 的末尾 0 值 |
| charat(str, index) | 得到字符串 str 中第 index 个字符 |

## 财务

| 公式 | 说明 |
| --- | --- |
| getEnglishCurrency(mark,number) | 将数字金额转为英文文本描述 |
| getChineseCurrency(Object) | 将传入的字符串或数字转换为大写金额 |
| toChinese(Object number,int flag1,int flag2) | 将传入的字符串或数字转换为中文 |
| setThMark(String) | 将传入的字符串或数字转为金额后加入千分位标志，如果希望保留数字后面的 0，则需要先将数字转为字符串，然后再用 setThMark() 函数，比如 setThMark(toString(56510.000)) |

## 控制公式

| 公式 | 说明 |
| --- | --- |
| $confirm->iif(,,"") | 确认提示，确定后继续执行公式，反之中止。举例 $Confirm->iif(a>2,"aaa","bbb"); 如果 $Confirm 变量的值不为""，则显示确认提示框，选择 OK，公式继续执行，否则公式停止执行，返回 false |
| $editable->iif(,"y","n") | 可编辑性控制公式。举例 $Editable->iif(item2>3,“Y”,“N”)； |
| $editable\[itemkey\]->iif(,"y","n") | 可编辑性控制公式。举例 $Editable\[itemkey\]->iif(item2>3,“Y”,“N”)； |
| $enabled->iif(,"y","n") | 有效性控制公式。举例 $Enabled->iif(item2>3,“Y”,“N”)； |
| $enabled\[itemkey\]->iif(,"y","n") | 有效性控制公式。举例 $Enabled\[itemkey\]->iif(item2>3,“Y”,“N”)； |
| $Error->iif(,,"") | 错误提示，中止公式执行。举例 $Error->iif(a>2,"aaaa","bb"); 如果 $Error 变量的值不为""，则显示错误提示框，公式停止执行，返回 false。 |
| $foreGround->iif(,"red","blue") | 前景色设置公式。举例 $ForeGround->iif(item2>3,“red”,“blue”)； |
| $foreGround\[itemkey\]->iif(,"red","blue") | 前景色设置公式。举例 $ForeGround\[itemkey\]->iif(item2>3,“red”,“blue”)； |
| $message->iif(,,"") | 消息提示，确定后继续执行公式。举例 $Message->iif(a>2,"aaa","bb"); 如果 $Message 变量的值不为""，则显示提示框，公式继续执行。 |

## 公式书写规介绍

这里以 getresult() 为例，该公式的功能是计算两个数的和。

![](https://community.yonyou.com/datadict/bipbook/assets/4-1.bYGpcANj.png)