---
name: 封装查询方法根据条件查询某字段
description: >
  用友 BIP 客开技能。当用户需要使用 QuerySchema + IBillQueryRepository 封装通用查询方法，根据条件查询单字段值时，参考该文档内容。
---

# 封装查询方法，根据条件查询某字段

```java
/**
 * 根据查询条件查询数据
 *
 * @param metaDataEnum        元数据枚举 uri
 * @param field               查询字段
 * @param queryConditionGroup 查询条件
 * @return 字段值
 */
public static String queryField(MetaDataEnum metaDataEnum, String field, QueryConditionGroup queryConditionGroup) {
    QuerySchema querySchema = QuerySchema.create();
    querySchema.addSelect(field);
    querySchema.addCondition(queryConditionGroup);
    try {
        List<Map<String, Object>> data = UBaseContext.getBean(IBillQueryRepository.class)
                                                     .queryMapBySchema(metaDataEnum.getUri(), querySchema, metaDataEnum.getDomain());
        if (CollectionUtils.isNotEmpty(data)) {
            return BizObject.fromMap(data.get(0)).getString(field);
        }
        return null;
    } catch (Exception e) {
        throw new RuntimeException(e);
    }
}

// 使用示例
QueryConditionGroup orgQueryConditionGroup = new QueryConditionGroup(ConditionOperator.and);
orgQueryConditionGroup.appendCondition(QueryCondition.name(field).eq(fieldValue));
orgQueryConditionGroup.appendCondition(QueryCondition.name("dr").eq(0));

queryField(metaDataEnum, field, orgQueryConditionGroup);
```
