---
name: 旗舰版翻译交易类型
description: >
  用友 BIP 客开技能。当用户需要通过交易类型编码翻译交易类型 ID，使用 ITransTypeService 时，参考该文档内容。
---

# 旗舰版翻译交易类型

```java
@Resource
private ITransTypeService transTypeService;

String bustype = "G001";
if (StringUtils.isNotBlank(bustype)) {
    TransTypeQueryParam param = new TransTypeQueryParam();
    param.setTransTypeCode(bustype);
    param.setTenantId(InvocationInfoProxy.getTenantid());
    try {
        List<TransType> list = transTypeService.queryMultiLTransTypes(param);
        if (CollectionUtils.isNotEmpty(list)) {
            data.put("bustype", list.get(0).getId());
        }
    } catch (TransTypeRpcException e) {
        throw new BusinessException("999", e.getMessage());
    }
}
```
