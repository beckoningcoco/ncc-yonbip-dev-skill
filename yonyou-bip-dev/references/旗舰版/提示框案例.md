---
name: 提示框案例
description: >
  用友 BIP 客开技能。当用户需要使用提示框（Toast 轻提示、Confirm 确认框、React.createElement 自定义内容），包括 cb.utils.alert 和 cb.utils.confirm 的各种用法时，参考该文档内容。
---

# 提示框案例

## 轻提示 Toast

```js
cb.utils.alert({
  title: '我是标题',
  type: 'info',       // 'error' | 'fail' | 'success' | 'warning' | 'info' | 'noIcon'
  duration: '3',      // 自动关闭延时（秒）
  mask: true,         // 是否显示透明蒙层
  onClose: () => { cb.utils.alert('关闭了') },
});
```

## 询问提示 Confirm

### 用法一：简单回调

```js
cb.utils.confirm(
  '确定删除吗?',
  () => { console.log('确定回调'); },
  () => { console.log('取消回调'); }
);
```

### 用法二：自定义按钮

```js
cb.utils.confirm({
  title: '确定下载吗',
  content: '下载内容...',
  iconType: 'info',
  footer: [
    { text: '上传并下载', type: 'primary', onPress: () => {} },
    { text: '直接下载', onPress: () => {} },
    { text: '取消', onPress: () => {} }
  ]
})
```

### 用法三：React.createElement 自定义内容

```js
cb.utils.confirm({
  title: React.createElement('div', { className: 'class1' }, '确定执行操作吗？'),
  content: React.createElement('div', { style: { color: 'red', whiteSpace: 'pre-wrap' } },
    '1、事项一。\n2、事项二。\n3、事项三。'
  ),
  okFunc: () => { console.log('确定回调'); },
  cancelFunc: () => { console.log('取消回调'); },
});
```
