---
name: 查询特征字段所在的表
description: >
  用友 BIP 客开技能。当用户需要查询某个特征字段（弹性字段）所在的表，或通过 YonQL 查询 elastic_field 获取字段归属信息时，参考该文档内容。
---

# 查询特征字段所在的表

```sql
-- 服务域.elastic_field 条件是字段名称/编码
SELECT vcol41 from yonbip_clm_contract.elastic_field where field_name = 'WBS'
```

## 查询特征字段

```java
QuerySchema querySchema = QuerySchema.create()
    .addSelect("staffChangeDefines.id,staffChangeDefines.oalcbm,staffChangeDefines.oaspzt,...")
    .appendQueryCondition(QueryCondition.name("id").eq(billId));
List<? extends IBillDO> queryList = billQueryRepository.queryBySchema("ss.mddstaffchange.staffchange",
        querySchema, "hrcloud-corehr-process");
```

![image-20250903102123098](查询特征字段所在的表/image-20250903102123098.png)

staffChangeDefines.特征
